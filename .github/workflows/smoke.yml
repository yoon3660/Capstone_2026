name: CI smoke test

# 일시적인 진단용. ci.yml 이 startup_failure 로 죽는 원인이 워크플로 파일인지
# 계정/리포 설정인지 가르기 위한 최소 워크플로다.
#   - 이것도 0초에 죽으면 -> 파일 문제가 아니다 (Actions 설정 또는 결제)
#   - 이것만 통과하면  -> ci.yml 안에 GitHub 이 거부하는 것이 있다
# 판정이 끝나면 삭제한다.

on:
  push:
    branches:
      - develop/26

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello
